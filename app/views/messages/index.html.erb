<h1>Last Messages</h1>
<ul>
  <% @messages.each do |m| %>
    <li><%= m.peer.email %> works on
    <a href="https://bugzilla.redhat.com/show_bug.cgi?id=<%= m.bug.id %>" title="Bugzilla"><%= m.bug.id %></a>
    <%= link_to 'Delete', message_path(m), :method => :delete, :data => {:confirm => 'Are you sure?'} %>
    </li>
  <% end %>
</ul>
